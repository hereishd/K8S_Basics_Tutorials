# Installing All K8S requirements

## Install a VM

* Install Ubuntu on VirtualBox
* Make shure that sudo is install
  * if sudo is not installed:
  * ```$ su```
  * ```$ apt update && apt upgrade```
  * ```$ apt install sudo```
  * ```$ usermod -aG sudo <username>```
  * ```$ reboot```